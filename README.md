# @khoralabs/agent-capabilities

Composable toolkits, policy gates, and deterministic capability fingerprints for agents.

See [`packages/capabilities/README.md`](packages/capabilities/README.md) for API docs and examples.

```bash
bun install
cd packages/capabilities && bun test
```
